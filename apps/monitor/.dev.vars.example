# Copy to .dev.vars for `pnpm dev`. Prod: `wrangler secret put <NAME>`.
COOLIFY_API_TOKEN=
DISCORD_WEBHOOK_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Portfolio Neon (= packages/drizzle DATABASE_URL) — monitor_log evidence table
MONITOR_LOG_DB_URL=
