NEXT_PUBLIC_API_URL= # DEFAULT FOR ALL APPS


# Agency API worker (server-side only — never NEXT_PUBLIC)
AGENCY_API_URL= # https://api.alisamadii.com in prod, http://localhost:8787 for wrangler dev
AGENCY_ADMIN_API_KEY= # ak_ser_ key owned by an admin user — mint via `pnpm --filter api admin:key`
